import std;
using namespace std;

int main() {
  vector<double> v; // v starts of empty; that is, v has no elements

  v.push_back(2.7); // add an element 2.7 to v at the end (back)

  v.push_back(5.6); // add an element 5.6 to v at the end (back)
}
